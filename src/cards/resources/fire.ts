import { Card } from "../../models/card";
import { decks, cards } from "../../state";

export class Fire extends Card {
  static displayName = "Fire";
  static artUrl = "./assets/cards/fire.jpg";
}
